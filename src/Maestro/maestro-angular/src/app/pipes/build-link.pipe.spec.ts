import { BuildLinkPipe } from './build-link.pipe';

describe('BuildLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
