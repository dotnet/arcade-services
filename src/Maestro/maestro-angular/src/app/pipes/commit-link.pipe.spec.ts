import { CommitLinkPipe } from './commit-link.pipe';

describe('CommitLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new CommitLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
